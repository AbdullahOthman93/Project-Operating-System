Reserved for CI/CD workflow configuration. This is an Execution-layer, technology-specific artifact.

No workflows exist yet. Any automation added here must implement a contract from `docs/contracts/`. The Execution Contract such automation would implement is currently deferred until first need — see `docs/contracts/ContractModel.md`. Automation implements contracts; it never defines checks of its own.
