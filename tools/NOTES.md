Reserved for execution adapters that implement the Execution Contract defined in `docs/contracts/`.

No adapters exist yet. Each adapter added here must be traceable to a specific contract requirement — adapters implement contracts, they do not define new ones.
