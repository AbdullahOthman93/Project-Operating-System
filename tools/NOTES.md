Reserved for execution adapters. This is an Execution-layer, technology-specific artifact.

No adapters exist yet. Each adapter added here must implement a contract from `docs/contracts/`. The Execution Contract such adapters would implement is currently deferred until first need — see `docs/contracts/ContractModel.md`. Adapters implement contracts; they never define new ones.
